query {
    repository(owner: "%s", name: "%s") {
      object(expression: "master") {
        ... on Commit {
          history {
            nodes {
              committedDate
              additions
              deletions
            }
          }
        }
      }
    }
  }
